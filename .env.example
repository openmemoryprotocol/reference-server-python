# --- Signature demo defaults ---
# Key id used in Signature-Input
OMP_SIG_KEYID=sig1
# Public key for the server to verify (base64url, no padding)
# Fill this after you run scripts/sign_post_objects.py --gen-key once.
OMP_SIG_PUB_sig1=

# Signature mode: off | permissive | strict
OMP_SIG_MODE=strict

# Leave fallback off by default (safer). Enable only for dev exploration.
OMP_SIG_ENV_FALLBACK=0
# OMP_SIG_DEBUG=1
